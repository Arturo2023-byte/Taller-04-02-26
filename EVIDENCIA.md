# Evidencia - Taller Docker (Nginx + Flask + Postgres)

## Estructura del proyecto
- `proxy/` (Nginx)
- `webapp/` (Flask)
- `database/` (PostgreSQL)
- `docker-compose.yml`

## Comandos ejecutados

### Levantar servicios
```bash
docker compose up --build -d
docker compose ps
